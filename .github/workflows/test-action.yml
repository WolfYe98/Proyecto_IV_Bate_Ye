name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: tests
        run: docker run -t -v `pwd`:/test wolfye98/proyecto_iv_bate
