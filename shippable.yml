#Especifico que usaré node.js
language: node_js
#Las versiones en las que probaré son en versión 10 y 14.
node_js:
  - "10"
  - "14"
#Instalo las dependencias, el gestor de tareas y ejecuto los tests.
services:
  - docker
build:
  ci:
  #  - npm install
  #  - npm install -g gulp
  #  - gulp test
    - echo $SHIPPABLE_BUILD_DIR
    - cat $SHIPPABLE_BUILD_DIR/gulpfile.js
    - ls $SHIPPABLE_BUILD_DIR/test
  #  - docker run -t -v $SHIPPABLE_BUILD_DIR wolfye98/proyecto_iv_bate
